const {count,sides}= require("../lib/parse-args");
const {assert} = require("chai");

describe.skip("roll",()=>{
    describe("count",()=>{
        it("should return sides = arg[3] if arg[3] exists",()=>{
            assert.equal(arg[3] = array[3])
        });
    });
});